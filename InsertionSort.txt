static List<int> insertionSort(List<int> list)
 {
    
     int j;

     for (int i = 1; i < list.Count; i++)
     {
         j = i - 1;
         while (j>=0&&(list[j+1] < list[j]))
         {
             Swap(ref list, j+1, j);
             j--;
         }
     }
     return list;
 }