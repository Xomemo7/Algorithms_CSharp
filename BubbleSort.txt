static List<int> bubbleSort(List<int> list)
 {
     bool isSorted=false;
     while(!isSorted)
     {
         isSorted=true;
         for (int i = 0; i < list.Count - 1; i++)
             if (list[i] > list[i + 1])
             {
                 isSorted = false;
                 Swap(ref list, i + 1, i);
             }
     }
     return list ;

 }